# bob-015 (generation failed — no image file)

Attempted with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/leonardo/phoenix-1.0 --size=304x608 --filename=characters/bob-015.jpg
```

- **Model used:** `@cf/leonardo/phoenix-1.0`
- **Output:** none — request failed
- **Width/height honored:** Unknown (never reached generation)
- **Attempts:** 1 (by design — see below)

## Failure

HTTP 502:

```
AI generation failed: 3030: Input prompt contains NSFW content.
```

Only a single attempt was made here, deliberately: `bob-007.md`/`bob-011.md`
document that this **exact, unmodified** prompt was retried 18 times
against `phoenix-1.0` and failed all 18 — the only thing that has ever
worked for this model was reworded text (dropping "anime"/"57-year-old"/
"crazy", keeping explicit full-body framing). Since this run was instructed
to use the original prompt as given and only retry where success had been
previously observed *with that exact prompt*, this model was correctly
skipped for a retry loop; a single confirmatory attempt was made and it
failed as expected.
