# bob-008.jpg

Generated with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/bytedance/stable-diffusion-xl-lightning --size=300x800 --filename=characters/bob-008.jpg
```

- **Model used:** `@cf/bytedance/stable-diffusion-xl-lightning`
- **Output:** 304x800 JPEG, saved to `characters/bob-008.jpg`
- **Width/height honored:** Yes — 300 was rounded up to 304 (nearest multiple of 8), 800 kept as-is

## Note on `--size`

`--size=300x800` was honored: the worker clamped/rounded 300 to 304 and
returned a real 304x800 JPEG. However, the image content is **solid
black** — every one of the 243,200 pixels is exactly `(0, 0, 0)`, confirmed
via pixel-level inspection. HTTP status was 200, so this is a silent
failure, same symptom as `bob-005.jpg` (`@cf/stabilityai/stable-diffusion-xl-base-1.0`).
