# bob-006 (generation failed — no image file)

Attempted with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/leonardo/lucid-origin --size=300x800 --filename=characters/bob-006.jpg
```

- **Model used:** `@cf/leonardo/lucid-origin`
- **Output:** none — request failed
- **Width/height honored:** Unknown (never reached generation)

## Failure

HTTP 502:

```
AI generation failed: 3030: Input prompt contains NSFW content.
```

Leonardo's prompt filter flagged the prompt as NSFW. This is a false
positive (the same prompt succeeds on flux, sdxl, sdxl-lightning, and
dreamshaper); no image was produced or saved for this model.

**Resolved in [bob-010.md](bob-010.md):** this turned out to be a flaky,
probabilistic false-positive rate rather than a deterministic block on
specific wording — retrying the exact same unmodified prompt succeeded on
the 5th attempt. See `bob-010.md` for the full investigation.
