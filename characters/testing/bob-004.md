# bob-004.jpg

Generated with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/black-forest-labs/flux-1-schnell --size=300x800 --filename=characters/bob-004.jpg
```

- **Model used:** `@cf/black-forest-labs/flux-1-schnell`
- **Output:** 1024x1024 JPEG, saved to `characters/bob-004.jpg`
- **Width/height honored:** No

## Note on `--size`

Sending `width`/`height` (300/800) in the request body caused a hard failure,
consistent with `bob-003.jpg`:

```
AI generation failed: 5006: Error: Additional or unevaluated properties '/width, /height' at '/' not allowed
```

Retried with `width`/`height` omitted entirely, which succeeded and returned
the model's fixed 1024x1024 output. Image quality is good — a clean,
complete full-body character on an empty white background.
