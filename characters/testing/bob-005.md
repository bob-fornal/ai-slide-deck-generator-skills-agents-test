# bob-005.jpg

Generated with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/stabilityai/stable-diffusion-xl-base-1.0 --size=300x800 --filename=characters/bob-005.jpg
```

- **Model used:** `@cf/stabilityai/stable-diffusion-xl-base-1.0`
- **Output:** 304x800 PNG (saved with a `.jpg` extension per the requested filename — the bytes are actually PNG, not JPEG), saved to `characters/bob-005.jpg`
- **Width/height honored:** Yes — 300 was rounded up to 304 (nearest multiple of 8), 800 kept as-is

## Note on `--size`

`--size=300x800` was honored: the worker clamped/rounded 300 to 304 and
returned a 304x800 image. However, the response body is **solid black** —
every one of the 243,200 pixels is exactly `(0, 0, 0)`, confirmed via
pixel-level inspection, not a display artifact. HTTP status was 200, so
this is a silent failure: the model (or an upstream safety filter) appears
to have blanked the output rather than erroring.
