# bob-009.jpg

Generated with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/lykon/dreamshaper-8-lcm --size=300x800 --filename=characters/bob-009.jpg
```

- **Model used:** `@cf/lykon/dreamshaper-8-lcm`
- **Output:** 304x800 PNG (saved with a `.jpg` extension per the requested filename — the bytes are actually PNG, not JPEG), saved to `characters/bob-009.jpg`
- **Width/height honored:** Yes — 300 was rounded up to 304 (nearest multiple of 8), 800 kept as-is

## Note on `--size`

`--size=300x800` was honored and this model actually rendered real content
(85,218 unique colors, not a blank frame like `bob-005.jpg`/`bob-008.jpg`).
Quality has a notable artifact though: a glitchy circular/patterned shape
floating near the character's head, and the pose/framing is a bit uneven
in the tall 304x800 aspect ratio.
