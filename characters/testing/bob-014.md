# bob-014 (generation failed — no image file)

Attempted with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/leonardo/lucid-origin --size=304x608 --filename=characters/bob-014.jpg
```

- **Model used:** `@cf/leonardo/lucid-origin`
- **Output:** none — request failed on every attempt
- **Width/height honored:** Unknown (never reached generation)
- **Attempts:** 16, all failed

## Failure

Every attempt returned HTTP 502:

```
AI generation failed: 3030: Input prompt contains NSFW content.
```

This is the same unmodified prompt that succeeded for this model in
`bob-010.md` (on the 5th of 5 attempts, at a different requested size,
`300x800`). A retry loop was used here specifically because that prior
success was on record — but this time all 16 attempts failed. This
reinforces the `bob-010`/`bob-013` finding that Leonardo's filter (and
these models' silent-failure modes generally) behave probabilistically:
a documented past success does not guarantee retries will reproduce it
within a reasonable attempt budget. No image was produced for this model
at this size.
