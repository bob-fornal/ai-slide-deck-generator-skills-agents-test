# bob-017.jpg

Generated with the `/generate-image` command:

```
/generate-image Create a complete anime character: head, torso, arms, legs, and shoes (this is not a portrait, but a complete person): a bald 57-year-old with glasses and a crazy white beard, wearing a black hoodie and jeans. Nothing more; keep the background empty. --model=@cf/lykon/dreamshaper-8-lcm --size=304x608 --filename=characters/bob-017.jpg
```

- **Model used:** `@cf/lykon/dreamshaper-8-lcm`
- **Output:** 304x608 PNG (saved with a `.jpg` extension per the requested filename), saved to `characters/bob-017.jpg`
- **Width/height honored:** Yes — 304x608 requested, 304x608 returned exactly
- **Attempts:** 1 (succeeded first try, per policy — no track record of this model needing retries)

## Quality note

Reversal of the earlier `bob-009.jpg` result: that run on this same model
produced real (if glitchy) content, but this attempt came back **solid
black** — every one of the 184,832 pixels is exactly `(0, 0, 0)`, confirmed
via pixel-level inspection. Combined with `bob-013.jpg`/`bob-016.jpg`
flipping the other way (black → real content) on the same rerun, this
confirms the "200 OK but blank image" failure mode observed across the
SDXL-family models is flaky per-request, not a fixed trait of any one
model.
