---
description: Generate a slide image from a context statement via the generate-image skill
argument-hint: <context statement> [--type=jpeg|png] [--size=WxH] [--model=<workers-ai-model-id>] [--filename=path] [--image=path ...]
---

Invoke the `generate-image` skill (see
`.claude/skills/generate-image/SKILL.md`) with the arguments: $ARGUMENTS
