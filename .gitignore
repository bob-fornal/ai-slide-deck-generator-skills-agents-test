# Secrets / local config
.env
.dev.vars

# Node / Cloudflare Workers tooling
node_modules/
.wrangler/

# Generated output images
generated_image*.png
generated_image*.jpg

# Local output from scripts/test-reference-image.js — regenerate as needed,
# don't commit (can contain arbitrary reference images passed in via --reference).
test-output/

# OS
.DS_Store
